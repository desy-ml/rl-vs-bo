name: Retune Gamma
project: ares-ea-rl-a-new-hope
entity: msk-ipc
program: train.py
command:
  - ${env}
  - python3
  - ${program}
  - ${args}
method: grid
metric:
  goal: maximize
  name: eval/mean_reward
parameters:
  gamma:
    values: [0.2, 0.55, 0.8, 0.9, 0.98, 0.99,  0.999]
